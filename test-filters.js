const fs = require('fs');

async function run() {
  try {
    // We can fetch from the local server if it's running, or we can look at the raw data if we have it
  } catch (e) {
    console.error(e);
  }
}
run();
